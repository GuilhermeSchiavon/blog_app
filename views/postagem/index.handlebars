<div class="card text-left">
  <div class="card-body">
    <h4 class="card-title">{{postagem.titulo}}</h4>
    <hr>
    <p class="card-text">Data da Publicação: {{postagem.date}}</p>
    <hr>
    <p class="card-text">{{postagem.conteudo}}</p>
    <p class="card-text">{{postagem.descricao}}</p>
  </div>
</div>